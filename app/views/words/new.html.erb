<h1>新規投稿</h1>
<%= form_with model: Word.new do |f| %>
  <h4>ジャンル選択</h4>
  <%= f.select :genre, Word.genres_i18n.invert, {}, class: 'form-control' %>

  <h4>名言</h4>
  <%= f.text_area :content %>
  <h4>名前</h4>
  <%= f.text_field :name %>

  <%= f.submit '投稿' %>
<% end %>
